import React from 'react'

const FormComonent = ({label="",type="text",placeholder="",name,value,Handlechange}) => {
  return (
    <div>
       <div>{label}</div>
                <div>
                    <input
                    className='px-6 py-3 border-[2px]  border-black text-black rounded-lg w-full'
                     type={type} name={name} placeholder={placeholder} value={value} onChange={Handlechange} />
                </div>
    </div>
  )
}

export default FormComonent
